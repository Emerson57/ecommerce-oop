# Copie a ".env" (ignorado por Git) y sustituya cada valor en su secret manager o entorno de despliegue.
# Documentación: docs/production-secrets.md

MSSQL_SA_PASSWORD=<set-in-deployment>
MONGO_APP_USER=<set-in-deployment>
MONGO_APP_PASSWORD=<set-in-deployment>
JWT_SIGNING_KEY=<set-in-deployment-min-32-chars>
